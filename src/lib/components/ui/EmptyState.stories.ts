import EmptyState from './EmptyState.svelte';

export default { title: 'Design System/EmptyState', component: EmptyState };
export const Basic = {
	args: { title: 'No commands', body: 'Create a command to populate this list.' },
};
